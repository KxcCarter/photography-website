import styled from 'styled-components';

export const StyleWrapper = styled.div`
  padding: 20px 0;

  max-width: 400px;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;

  input {
    display: block;
    margin: 0.5rem 0;
    margin: auto;
  }

  .field {
    margin: 0.5rem;
  }

  button {
    margin-top: 1rem;
  }
`;
